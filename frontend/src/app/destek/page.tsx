import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Destek sayfası",
    description: "Destek sayfasının açıklmasıdır",
};

const DestekSayfa = () => {
    return (
        <div>
            Enter
        </div>
    );
}

export default DestekSayfa;